--- a/output_stdout.c
+++ b/output_stdout.c
@@ -113,6 +113,7 @@ static void *output_thread(void *vargp) {

 		if (buffill) {
 			fwrite(buf, bytes_per_frame, buffill, stdout);
+			fflush(stdout);  // Force flush when stdout is redirected to pipe
 			buffill = 0;
 		}

